# Does not support Wayland, I can't test if it works on X11 either
AUR=1 arch_x86_64 pelfCreator -m "xplshn" -n "org.onlyoffice.desktopeditors" -p "onlyoffice-bin" -e "onlyoffice-desktopeditors.desktop" -s
