# REASON: The AUR installs it to weird places, would require an AppRun specifically made for this AppBundle
AUR=1 arch_x86_64 pelfCreator -m "xplshn" -n "one.ablaze.floorp" -p "floorp-bin" -e "floorp.desktop" -s
